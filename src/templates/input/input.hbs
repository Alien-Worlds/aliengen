{{render "imports" imports print_markers=print_markers}}

type UnknownBody = unknown;
type UnknownParams = unknown;
type UnknownQuery = unknown;

{{#each models}}
{{> input this}}

{{/each}}
{{#if print_markers}}/*__input__*/{{/if}}