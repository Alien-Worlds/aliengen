{{render "imports" imports print_markers=print_markers}}

{{#if print_markers}}/*__mock__*/{{/if}}

{{#each models}}
describe('{{pascal_case name}}Input', () => {
  describe('fromRequest', () => {
    it('should create a {{pascal_case name}}Input instance with the correct parameters', () => {
      const request = {
        params: { success: true },
      };

      const input = {{pascal_case name}}Input.fromRequest(request as any);

      expect(input.success).toBe(true);
    });
  });

  {{#if print_markers}}/*__unit_test__*/{{/if}}
});
{{/each}}
{{#if print_markers}}/*__group__*/{{/if}}
