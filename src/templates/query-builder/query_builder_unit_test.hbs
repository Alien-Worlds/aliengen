{{render "imports" imports print_markers=print_markers}}

{{#if print_markers}}/*__mock__*/{{/if}}

{{#each models}}
describe('{{pascal_case name}}{{pascal_case type}}QueryBuilder', () => {
  describe('build', () => {
    it('should create a Query type object', () => {
      const queryBuilder = {{pascal_case name}}{{pascal_case type}}QueryBuilder();
      /*

      queryBuilder.with({ key1: 'lorem', key2: 'ipsum' });
      
      */
      const query = queryBuilder.build();

      expect(query).toBe({});
    });
  });

  {{#if print_markers}}/*__unit_test__*/{{/if}}
});
{{/each}}
{{#if print_markers}}/*__group__*/{{/if}}
