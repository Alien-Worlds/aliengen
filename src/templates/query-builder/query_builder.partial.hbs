/**
 * Query builder class for constructing a query to ...
 *
 * @class {{pascal_case name}}{{pascal_case type}}QueryBuilder
 * @extends {QueryBuilder}
 */
export class {{pascal_case name}}{{pascal_case type}}QueryBuilder extends QueryBuilder {
  /**
   * Build the query to ...
   *
   * @public
   * @override
   * @returns {Query} The constructed ... query.
   */
  public build(): Query {
    return {};
  }
}