{{#if print_jsdocs}}
/**
 * A service class for ...
 *
 * @class {{pascal_case name}}ServiceImpl
 * @implements { {{pascal_case name}}Service }
 */
 {{/if}}
export class {{pascal_case name}}ServiceImpl implements {{pascal_case name}}Service {
  {{#if (not_empty props)}}
  {{#if print_jsdocs}}
  /**
   * Creates an instance of {{pascal_case name}}ServiceImpl.
   *
   * @constructor
   {{#each props}}
   {{> jsdoc_param this}}
   {{/each}}
   */
   {{/if}}
  constructor(
    {{#each props}}
    {{> constructor_prop this}}
    {{/each}}
  ) {}
  {{/if}}

  {{#each methods}}
  {{> method this}}
  {{/each}}
}
