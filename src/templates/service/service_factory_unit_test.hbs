{{>imports imports}}

{{#if print_markers}}/*__mock__*/{{/if}}
{{#each imports}}
{{> mock_import this}}

{{/each}}
const containerMock = {
  bind: jest.fn(),
} as any;
{{#each models}}
describe('{{pascal_case name}}ServiceFactory', () => {
  it('should create and initialize {{pascal_case name}}Service', async () => {    
    const databaseMock = {};
    const repository = await {{pascal_case name}}ServiceFactory.create(containerMock);

    expect(repository).toBeInstanceOf({{pascal_case name}}Service);
    expect(containerMock.bind).toHaveBeenCalledTimes(1);
  });
  {{#if print_markers}}/*__unit_test__*/{{/if}}

});

{{/each}}
{{#if print_markers}}/*__group__*/{{/if}}
