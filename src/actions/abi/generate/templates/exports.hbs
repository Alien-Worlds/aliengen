{{> warn-do-not-edit}}

import 'reflect-metadata';

{{#each exports}}
    {{#if exportAs}}
export * as {{exportAs}} from '{{path}}{{../suffix}}';
    {{else}}
export * from '{{.}}{{../suffix}}';
    {{/if}}
{{/each}}