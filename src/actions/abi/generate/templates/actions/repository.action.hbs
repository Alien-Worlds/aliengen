{{! template for action repository 
    e.g. src/contracts/dao-worlds/actions/domain/repositories/dao-worlds-action.repository.ts }}
{{> warn-do-not-edit}}
import { 
  ContractAction, 
  ContractActionDocument, 
  Repository, 
  injectable, 
} from '@alien-worlds/api-core';

import { DataDocumentType } from '../../data/dtos/{{paramCase contract}}-action.dto';
import { DataEntityType } from '../../domain/entities/{{paramCase contract}}-action';

/**
 * @description This abstract class represents a repository for `{{contract}}` contract actions.
 */
@injectable()
export abstract class {{pascalCase contract}}ActionRepository extends Repository<
  ContractAction<DataEntityType, DataDocumentType>,
  ContractActionDocument<DataDocumentType>
> {
  public static Token = '{{constantCase contract}}_ACTION_REPOSITORY';
}
