{{! template for delta repository 
    e.g. src/contracts/dao-worlds/deltas/domain/repositories/dao-worlds-delta.repository.ts }}
{{> warn-do-not-edit}}
import { 
  Contract{{pascalCase actionOrDelta}}, 
  Contract{{pascalCase actionOrDelta}}Model, 
  Repository, 
  injectable, 
} from '@alien-worlds/aw-core';

import { DataDocumentType } from '../../data/dtos/{{paramCase contract}}-{{actionOrDelta}}.dto';
import { DataEntityType } from '../../domain/entities/{{paramCase contract}}-{{actionOrDelta}}';

/**
 * @description This abstract class represents a repository for `{{contract}}` contract {{actionOrDelta}}s.
 */
@injectable()
export abstract class {{pascalCase contract}}{{pascalCase actionOrDelta}}Repository extends Repository<
  Contract{{pascalCase actionOrDelta}}<DataEntityType, DataDocumentType>,
  Contract{{pascalCase actionOrDelta}}Model
> {
  public static Token = '{{constantCase contract}}_{{upperCase actionOrDelta}}_REPOSITORY';
}
