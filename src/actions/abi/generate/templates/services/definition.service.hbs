{{! template for service definition e.g. src/contracts/dao-worlds/services/dao-worlds-contract.service.ts }}
{{> warn-do-not-edit}}
import {
  GetTableRowsOptions,
  Result,
  SmartContractService,
} from '@alien-worlds/aw-core';

import { 
{{#each deltas}}
  {{ pascalCase .}}RawModel,
{{/each}}
} from '../deltas/data/dtos';

export abstract class {{pascalCase contract}}ContractService extends SmartContractService {
  public static Token = '{{constantCase contract}}_CONTRACT_SERVICE';

{{#each deltas}}
  public abstract fetch{{pascalCase .}}(
    options?: GetTableRowsOptions
  ): Promise<Result<{{pascalCase .}}RawModel[], Error>>;
{{/each}}
}
