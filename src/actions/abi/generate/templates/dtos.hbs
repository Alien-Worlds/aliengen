{{! template for action dtos e.g. src/contracts/index-worlds/actions/data/dtos/setstatus.dto.ts }}
{{> warn-do-not-edit}}
import { MongoDB } from '@alien-worlds/api-core';
{{> imports}}

export type {{actionName}}Document = {
    _id?: MongoDB.ObjectId;
{{#each props}}
    {{this.key}}?: {{this.entityType.name}};
{{/each}}
    [key: string]: unknown;
};

export type {{actionName}}Struct = {
{{#each props}}
    {{this.key}}?: {{this.structType.name}};
{{/each}}
    [key: string]: unknown;
};
