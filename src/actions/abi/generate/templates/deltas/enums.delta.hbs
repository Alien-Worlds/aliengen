{{! template for delta enums e.g. src/contracts/dao-worlds/deltas/domain/enums.ts }}
{{> warn-do-not-edit}}
/**
 * @enum
 * @readonly
 * @description {{pascalCase contract}} Table Names
 */
export enum {{pascalCase contract}}TableName {
{{#each deltas}}
    {{pascalCase .}} = '{{ . }}',
{{/each}}
};
