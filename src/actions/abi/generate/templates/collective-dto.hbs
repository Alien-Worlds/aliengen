{{#each dtos}}
import { {{pascalCase .}}Document } from './{{.}}{{../suffix}}';
{{/each}}

export type DataDocumentType =
{{#each dtos}}
  {{pascalCase .}}Document 
  {{~#unless @last}} |{{/unless}}
  {{~#if @last}};{{/if}}
{{/each}}